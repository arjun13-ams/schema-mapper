# Schema Mapper & Code Generator

## Setup
```
npm install
npm run dev
```

## Usage
1. Select input/output file types (XML/JSON)
2. Upload schema files
3. Click input field, then output field to map
4. Or click output field to enter constant value
5. Generate Python or XSLT code
